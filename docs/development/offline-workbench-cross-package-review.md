# Workbench preflight review note

This file is intentionally temporary pending separation into its own feature branch. The workbench code must be independently tested and reviewed and must not be treated as a dependency or acceptance of Discord networking.
