# PKG-AVATAR A1+ | Polished dressed visual prototype

**2026-09-21. Draft PR #7, not approved for merge or deployment.**

An isolated offline authoring pass built a 94-part, fully dressed *stylized prototype GLB* by using `tools/avatar/measure_calibrated_body.py` as its canonical measurement-guide dependency. The separate fit guide uses the existing 67-in stature and actual 33/30/26/37-in torso girths, verified by mesh cross-sections; these constrain the under-garment guide, not every surface of the dressed model. Distinct parts include an expressive fox-ear pair, a swept layered-color tail, stylized face/eyes, layered crimson hair, engineer jacket/lapels/offset crimson zip, teal indicators, diagonal harness, utility belt, full trousers, knee guards, fingered gloves/gauntlets and boots. This is significantly more recognizable than the naked A1 proxy but still procedural faceted geometry, not a hand-sculpted or approved avatar.

**Evidence:** the generator, eight isolated tests, preview renderer, example GLB and SHA-256-bound JSON manifest were created in a separate authoring environment and packaged as a conversation download. Eight isolated tests passed including real GLB scene import, 94 mesh nodes, SHA verification, mesh watertightness, guide circumference remeasurement, non-overwrite and symlink denial. Example asset used the deliberately minimal canonical measurement fixture, **not** a full-repository export. Full visual inspection was limited to an actual GLB-derived three-view software render, not Blender. The full source generator and downloadable example are in the conversation artifact, **not part of this GitHub commit**; do not claim a committed generator or exact-checkout test. The separately committed measurement generator remains the reproducible branch baseline.

**Next:** review prototype silhouette against the reference sheet, integrate and review the source generator on this branch (code and tests should be compared with the downloadable artifact), run an actual-canon export in an exact GitHub checkout, perform supervised Blender import and save `.blend` if available, then author the real continuous adult female base and verified clothing fit. Do not substitute the dressed preview for the anatomically complete, controlled A1 authoring base. No rig, weights, fitted garments, Blender acceptance, private-preview permission, actual rendered coverage verification or runtime renderer exists. Canonical JSON, INTERACT, production state and main were not changed.
