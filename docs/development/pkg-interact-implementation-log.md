# PKG-INTERACT: implementation log and acceptance gates

## Authoritative lab clarification

**Sparks's lab is a place Sofía can enter and work in, not the simulator.** [World-location contract](pkg-interact-world-location-contract.md) supersedes previous language equating the two. `LabWorld` is a persistent virtual location with real software state and action outcomes. `InteractionLab` is a synthetic, isolated **test harness**; it is never proof of actions in Sofía's actual lab, physical presence or real equipment operation. Text and future avatar clients must share world object IDs, transitions and evidence, as they already share canonical body-interaction semantics.

## Slice I1: shared headless body-interaction semantics (focused tests passed)

The existing canonical `Embodiment` drives `InteractionEngine`. Explicit fox ears and tail are checked against its feature/anatomy data; human body part names are **form-derived coverage**, not proof of rendered geometry. The versioned registry contains left/right parts, ear/tail subregions, and explicitly restricted regions. Unknown forms or new canonical appendages fail visibly instead of disappearing.

One `InteractionEvent` and one `InteractionDecision` serve `from_text` and `from_lab_pointer`. Equal actor/region/gesture/phase produces equal semantic meaning, policy status and candidate modeled reactions for supported fixtures. Source, event ID and evidence refs differ. Pointer inputs are already-resolved *synthetic* gestures; a real mouse click is not magically a pat. Real hit testing, authenticated input, renderer acknowledgments and physical sensation are not supplied.

`InteractiveConversationService` extends `EmotionalConversationService` and adds trusted decision context to the LLM request without altering the original persisted message or introducing a second bot, emotional journal or independent persona. Textual `*...*` cues are optional modeled expression, not confirmed renderer output or subjective feeling. The short, explicitly addressed text grammar abstains on discussion, negation, hypotheticals, quotes, code and composite phrases. Ambiguous regions clarify; private regions deny by default; no adult-mode authorization is implied. Existing narrow head-pat journaling remains; new durable body-interaction events are **not** implemented or accepted.

**Observed local verification supplied by Sparks:** On Windows, branch `feature/pkg-interact-shared-engine` at its I1 revision `7175835`, the command `pytest -q -x test/test_interaction_shared_engine.py test/test_interaction_chat_projection.py test/test_application.py test/test_affection_cue_phrasings.py` reported **59 passed in 51.61 seconds**. Focused only, not a full suite, live Ollama review or renderer verification. The user's locally modified `state/sofia.db` was preserved during branch switching.

## Slice I2: isolated interaction simulator (committed, tests pending)

`src/sofia/interaction/lab.py` provides an in-process `LabScene`/`LabStep`/`InteractionLab` with validated synthetic inputs, distinct IDs, chronological order, 64-step maximum, deterministic replay, stop barrier and redacted decision-only trace export. It exercises the **same body `InteractionEngine`** for explicit text and simulated resolved pointer inputs. The fixture is never persisted or forwarded as genuine user evidence; stop state is local to each scene. This is **not Sofía's lab location**, durable production revocation or authenticated UI input. `test/test_interaction_lab.py` is committed and has not yet been reported run by Sparks.

## Slice I3: persistent virtual lab location and narrow text route (committed, tests pending)

- `src/sofia/interaction/world.py` adds `LabWorld`, durable SQLite room/actor/object records and an atomic request/audit log. Explicit trusted authoring defines rooms and objects; no object is created merely because a user narrates it. A unique request ID returns the same result on replay and rejects changed payload. Enter/leave, pick up/put down, begin/finish virtual equipment work have state checks and visible denials. Equipment `work_finished` is NOT a verified repair.
- `src/sofia/interaction/world_setup.py` defines an explicitly authored starter **virtual** room `lab`, actor `sofia`, `screwdriver` and `scope` (oscilloscope). Provisioning occurs only on a recognized addressed lab command; Sofía must explicitly enter before handling objects. Existing state is preserved and conflicting definitions fail. A separate `sofia-lab.db`-style file beside the configured state DB keeps location data out of the conversation database; include it in future PKG-MEM/SAFE backup and deletion policy.
- `src/sofia/interaction/world_text.py` accepts narrow, explicit requests such as `Sofía, enter the lab`, `Sofía, pick up the screwdriver` and `Sofía, work on the oscilloscope with the screwdriver`. `Sofía is working on some lab equipment` is narration and **does not execute work** or prove equipment state. Unknown items or missing tool IDs clarify. Only a previously saved user message can supply the source ID through the conversation service; the LLM cannot convert proposed prose into a completed action.
- `InteractiveConversationService` reuses the existing service and routes recognized saved text commands into the same `LabWorld.perform(WorldAction)` a future independently authenticated avatar controller must use. Prompt context describes the committed world outcome and current object state rather than fabricating actions. No renderer is required for text use. Dedicated offline tests cover persistence/restart, replay, denial, progression, narration abstention, text versus synthetic resolved-avatar world-action equivalence and conversation integration.

**Verification status:** I3 tests `test/test_interaction_world_location.py` and `test/test_interaction_world_text.py` are committed but **not run on Sparks's Windows checkout**. No live CLI/model review. World kernel has no real UI authentication, independently enforced user/personal consent or stop, actual graphics, autonomous agent policy, physical equipment or network adapter. No simulator result counts as a production world event. This slice is not whole-PKG-INTERACT acceptance.

## Remaining acceptance and nonclaims

- Run focused I2/I3 tests and representative I1/application regressions; fix any genuine failures without weakening assertions. Inspect one supervised real CLI flow: enter lab, pick up tool, work on named equipment, finish, restart and inspect state; also try narration and denied operations. Review tone separately from correctness.
- Improve grammar and aliases without treating anatomy discussion or lab narration as completed touch/work. Audit every canonical body part and clothing layer. Build source-linked, privacy-aware durable body-interaction records with the existing EmotionalJournal, avoiding double head-pat records or test fixtures.
- Implement independently enforced stop/permissions and duplicate-event protection for actual multi-client input; separate Sofía's proposed autonomous lab actions from user commands. A generic model response must not directly operate equipment or change trusted world state.
- For the real avatar: choose renderer and trusted input channel, validate geometry/hit tests, occlusion, click-through, gesture sequences and renderer acknowledgments. Map avatar object IDs and actions into **the same** world engine and body events. No separate personality, lab, or screwdriver per mode.
- External screen operations, intimate-mode handling, voice and hardware require their own authority gates. Full pytest remains deferred until after INTERACT as requested; PKG-MEM follows package acceptance.
