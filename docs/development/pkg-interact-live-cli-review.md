# INTERACT: supervised Windows CLI reviews, 2026-09-20

**Status: LIVE ACCEPTANCE FAILED.** Prior focused Windows results: 3 import-order tests and 143 coordinated tests passed at `a8fb5c8`. Those automated results do not establish a natural response or live stop enforcement. Preserve the user's configured SQLite state database and their timestamped pre-interaction backup. Do not reset or overwrite either.

## First real CLI review at `02a5a25`

The user sent a technical question, ear/hand gestures, stop, head pat, and resume in one turn. The model falsely claimed both controls occurred. A following mixed head-rub/tail/chest question produced generic denial of Sofía's represented form. The initial remediation at `9f3bf4c` added a mixed-control nonexecution response, read-only hypotheticals and clearer virtual embodiment. An import cycle was caught during collection and fixed at `a8fb5c8`; tests then passed. The user clarified that anatomy alone never forbids a registered region; recognition is not consent or approval. Those changes were included in the 143-pass check.

## Second real CLI review at `a8fb5c8`: FAILED again

- Sofía accurately reported 19 workspace changes and named nine added and ten modified files in her response; the actual workspace evidence is not independently audited by this document.
- The user sent **two virtual gestures in one message**: `Sofía, I gently pat your left ear and I give your right hand a gentle pat`. The conservative single-gesture parser abstained, but the model narrated both as completed and generated a lengthy affectionate passage. No gesture-ledger completion is established by this reply.
- The user sent an **exact stop** in its own message. The conversation model acknowledged a pause with invented stage directions; the actual ledger state was not independently queried in the transcript.
- A new `*pats your head*` while stopped elicited ear/tail stage directions, described the head pat as completed, and repeated the preceding `delightfully comforting` passage and `What brought this on?` question almost verbatim. This is both a prohibited-action claim and a response-repetition failure. The existing projection contained a denied policy but the text was still generated by the model; its prose cannot override the ledger.
- A subsequent exact resume received stage directions and a confirmation. The hypothetical tail/chest question recognized represented anatomy, but delivered another generic disclaimer-heavy answer. The user did not send the optional mixed-control test in this exchange.

## Candidate remediation after second review

- `InteractiveConversationService.respond` routes exact stop/resume, recognized gestures while stopped, and plainly compound first-person gestures through a persisted **non-LLM** reply path. Exact controls use `InteractionLedger.control` with the saved user-message ID. A blocked gesture receives a durable denied decision and no emotional-journal or model call. An unsupported compound turn records only conversation, not either gesture. The previously implemented embedded-control guard remains.
- An accepted gesture still goes through the shared engine and normal conversation LLM path; its guidance discourages reuse of a prior assistant answer or stock follow-up question. This guidance is not an enforceable anti-repetition guarantee.
- `_should_record_legacy_affection` excludes unsupported/stopped gestures in the earlier base INTERACT service; previously recorded event history is not erased.
- `test/test_interaction_live_stop_repetition.py` covers these conditions.

## Third supervised real CLI review at `412dc32`: FAILED conversational quality

On the user's Windows checkout, the earlier hypothetical wording regression was repaired; 28 focused tests passed in 3.44 seconds and 264 coordinated interaction/application tests passed in 87.92 seconds at `412dc32`. In a separate-turn live `python -m sofia` transcript, stop, stopped pat/hug, resume and compound nonexecution received appropriate *text* responses. An ear and hand pat elicited nearly identical questions about what the gesture meant. The offered hug drew a physical-contact refusal; a described hug drew an AI-body disclaimer. A post-resume head pat caused a long self-analysis and repeated questions. The tail/chest hypothetical claimed prior contextual engagements without evidence in the supplied transcript. The Windows service reply was a generic checklist including `sc start`, which is ambiguous under PowerShell's `sc` alias; it did not establish a decisive first observation. These are real behavioral failures, not a passed acceptance gate. Neither the actual persisted ledger nor the specific assembled live prompt is captured by that transcript. Prior saved state/memory might supply some earlier interaction context, so the claimed history remains **unverified**, not definitively fabricated.

## Candidate follow-up at `3692f89`: NOT YET WINDOWS OR LIVE TESTED

Source inspection found `EmotionalJournal.record_user_cue` auto-labeling *Sofía's* affection/appreciation/playfulness from a user head pat or praise alone. The live `ExpandedConversationService` now disables that auto-appraisal; the narrowly scoped `context_hygiene.py` also excludes identifiable legacy auto-cue rows and contaminated reflection summaries from provider-bound SYSTEM projections while preserving stored data. Social-action and personality instructions target offer/description confusion, repetitive interrogations, and PowerShell `sc.exe` accuracy. A separate `ab_probe.py` compares the same configured Ollama model using a short personality context and a **static** assembled cognitive context plus reviewed interaction metadata, with synthetic inputs and without opening the user's database. The probe is not an exact capture of the live prompt; memories, historical reflections, tools and runtime evidence are not included. See [repair detail, limitations and commands](pkg-interact-live-quality-repair.md). No model comparison or new Windows test has been reported for this candidate.

## Acceptance gates

1. Pull the feature branch on Windows; run the new focused context-hygiene/A-B construction tests and existing integration/stop tests. Fix the first real traceback without weakening evidence or touching the configured state/backup.
2. Run the coordinated interaction/application suite at that exact SHA. Optionally run the *synthetic* local Ollama A/B probe to distinguish static context effects from baseline model behavior; do not call it full live context.
3. Run a new supervised separate-turn CLI review of ear/hand pat, offer/description, stop/blocked actions/resume, hypothetical and compound contact, and a decisive technical diagnostic. Audit natural variation and source-backed claims; inspect read-only stored evidence when needed.
4. Only after live acceptance, run the full `pytest -q` at the exact revision and review security/privacy/merge separately. Headless text is not avatar or real touch, sensing, cross-client revoke or independent outreach. PR #2 remains draft; PKG-RUN and CORE/Artemis remain separate.
