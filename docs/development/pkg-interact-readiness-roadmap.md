> **Live quality finding — 2026-09-24:** supervised Discord use exposed generic/canned assistant fallback in ordinary greetings even though canonical embodiment grounding remained correct. Accepted INTERACT safety, stop, ledger, source-attestation and interaction semantics stay accepted; a new narrow INTERACT/CORE live-quality repair gate owns natural/non-canned conversational expression.

> **Closure update — 2026-09-23:** PKG-INTERACT was accepted by Sparks and merged to `main` via PR #2 (merge commit `d6658d0`). Final verified Windows evidence: **97 focused tests**, a four-turn disposable real-application/Qwen probe, **1665 passed / 2 skipped / 1 deselected** in the qualified repository run (the deselected case was Sparks's unrelated local Ollama expectation mismatch), and a final **60/60 closure audit** including both SQLite writer orders, source attestation, revocation, restart-persistent interaction state, stop behavior, authentication and tool authority. Staged offers remain off by default; production interaction-policy schema provisioning is a separate reviewed migration. The active dependency gate is now **PKG-MEM**.

# PKG-INTERACT | accepted closure record

**Closed 2026-09-23 | PR #2 merged to `main` as `d6658d0`.** Existing detailed contracts: `pkg-interact-live-quality-repair.md`, `pkg-interact-conversation-projection-repair.md`, `avatar-screen-interaction-contract.md`, `pkg-interact-discord-channel-contract.md`. Do not replace their existing acceptance matrices. This document is now a historical readiness record. The completed closure evidence is summarized above; future work belongs to downstream packages.

## Prepared and actually observed

Text/headless interaction, typed canonical regions/policy and isolated lab have substantial branch code; bounded `ConversationalContextAssembler` provider-context candidate recently added. Older Windows SHA `23fe17d` had 26 focused and 274 coordinated tests passing. A supervised CLI at SHA `412dc32` **failed** natural personality, distinct gestures, offered-versus-described hug, hypothetical grounding and technical response quality. An A/B probe against qwen3:14b recorded ~3.3k-character minimal vs ~72–74k-character full static prompts with generic/ungrounded results in both arms; technical B reply absent. These are historical and **do not validate the current candidate**. Do not infer prompt length was sole cause.

## Work and acceptance sequence

1. **Preserve and pin:** Close CLI, protect the local `state/sofia.db`, timestamped backup and unrelated local `test/test_ollama_generation_contract.py` edit; obtain current branch SHA and baseline. Do not reset, recreate or migrate the live DB to make tests pass.
2. **Focused Windows regression:** `python -m pytest -q -x test/test_conversational_context_projection.py test/test_interaction_ab_probe.py test/test_interaction_context_hygiene.py test/test_interaction_expanded_service.py test/test_interaction_live_discussion.py`; inspect actual failures and rerun on the final changed SHA. Then run the coordinated interaction/application subset. Record counts/time/environment, not a previous branch result.
3. **Real model review:** repeat A/B with all answers, prompt sizes, matched cases and provider settings; run separate-turn live `python -m sofia` identity, head/ear/tail and other permitted body cues, denied/negated/hypothetical inputs, offer versus completed gesture, troubleshooting, affectionate and serious dialogue. Check unknowns, unique phrasing, actual reaction quality, no fictional touch or sensor claim. Quality is assessed on *actual replies*, not only unit tests.
4. **Cross-package CORE/SAFE:** review constitutional projection integrity, identity facts, long conversation history/token budget, provider switching, actual timestamp/provenance and permission denials. No silent constitutional edits, invented private thoughts, automatic restricted mode or autonomous outreach.
5. **Full-suite checkpoint after accepted live INTERACT:** `python -m pytest -q` on the pinned revision, record pre-existing failures/skips and solve without disabling tests. Review real startup/restart and migration on a disposable copy. Only then consider an explicit code merge.
6. **Later separately gated:** MEM durable original retrieval, authenticated Sparks-only Discord D0–D4, actual renderer pointer/gesture and self-animation acknowledgments, voice/screen executor, 24/7 RUN and general search in the agreed order. A headless success never proves rendered interaction.

## Open decisions to review at the gate

Check canonical region registry against `src/sofia/data/avatar.json`; define user-visible opt-in, retention and revocation for restricted regions without equating a model's words to consent. Determine eventual renderer coordinate/gesture phase contract, text-versus-animation fallback, screen-control approval and terminal UX. Discord actual account/bot credentials remain unconfigured and never stored in this roadmap. Resolve which original-message/MEM projection is permitted and review personality against a small reproducible conversation set.

**Exit status:** PKG-INTERACT accepted and merged. The merged interaction foundation is available to downstream packages. Renderer/Discord/voice/physical sensing remain separate package gates. Production staged-offer schema provisioning remains intentionally separate and not performed by the merge. [Master proposed readiness](../../MASTER-ROADMAP-READINESS.md) lives on draft PR #4, not this branch.
