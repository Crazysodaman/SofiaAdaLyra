# PKG-REL alternative | reconciliation roadmap

**2026-09-21 | draft PR #13 | baseline head `b8c98eabc04b57b4c6ce213f8d6b82a480793d29`.** This branch is an **alternative candidate**, not an additional REL package. Coordinate with [PR #12](https://github.com/Crazysodaman/SofiaAdaLyra/pull/12) and its `pkg-rel-readiness-roadmap.md` before any merge. No automatic supersession or destructive branch rewrite.

## What exists and what does not

Typed last-contact evidence, same-actor gap checks, optional non-clingy reunion cue and default-denied outreach candidate are proposed as offline code with dedicated tests. **No pinned GitHub checkout/Windows/full suite/CI or supervised personality result is reported for #13**, and no message is sent. Its relationship/contact data cannot become a second source of truth alongside CORE continuity, MEM provenance and #12.

## Required reconciliation

1. Pin both PR SHAs and compare event schemas, time/clock handling, duplicate/replay behavior, zero/unknown gaps, explicit stop, source provenance and phrasing. Reuse one contact pipeline and existing EmotionalJournal, not parallel stores.
2. Transfer missing tests/valuable semantics into the chosen reviewed branch without losing original IDs or assuming all differences are equivalent. Record a diff and migration/rollback if any persisted representation changes. Update master readiness index after decision.
3. Run the chosen code's tests on actual Windows checkout, negative cross-user/forged-event/restart/correction cases, and a supervised multi-session live model review. No claimed subjective thoughts, fabricated offline uptime or needy outreach.
4. Route any proposed DM via ACT opt-in, SAFE verified actor, quiet/stop controls, Discord delivery acknowledgement and later RUN evidence. Preserve general multi-user deferral.

## Open decision

Choose the canonical REL API and which branch survives after a side-by-side review of actual code and test evidence. Until then **both are draft and neither is merge-ready**. No protected identity/Constitution, state database, merge or deployment change.
